version: 2

sources:
  - name: raw
    schema: raw
    tables:
      - name: yellow_taxi_trips
        description: "One row per taxi trip. Loaded by the Python pipeline"
      - name: weather_hourly
        description: "One row per hour. Loaded by the Python pipeline"
